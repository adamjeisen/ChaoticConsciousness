# @package _global_
hydra:
  run:
    dir: /scratch2/weka/millerlab/eisenaj/ChaoticConsciousness/hyperparameter_testing/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: /scratch2/weka/millerlab/eisenaj/ChaoticConsciousness/hyperparameter_testing/${now:%Y-%m-%d}/${now:%H-%M-%S}
      